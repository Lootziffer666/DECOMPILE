import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.example.nextjsandroidapp",
  appName: "Next.js Android App",
  webDir: "out",
};

export default config;
